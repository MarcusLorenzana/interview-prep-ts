export class SlidingWindowCounter {
  record(timestamp: number): void {
    // TODO: append timestamps for incoming requests
  }

  count(timestamp: number): number {
    // TODO: count timestamps within the previous 300 seconds
    return 0;
  }
}
